# Python
__pycache__/
*.pyc

# OS / editor
.DS_Store
Thumbs.db
*.swp

# Local server logs / temp
*.log

# Editor / harness config
.claude/

# Build cache: regenerated by scripts/build_coverage.py via Valhalla; the live site
# does not use it (it reads data/coverage.geojson). Ignored to keep the repo lean.
data/iso_cache.json
